# Persoonlijke testdata — bevat mogelijk energieverbruiksdata
energy-3.csv
p1_sample.json
*.csv
*.json
# Uitzonderingen: dit zijn build-/config-bestanden, GEEN persoonlijke data, en ze MOETEN
# mee in de repo — anders kan een verse clone niet bouwen/testen. De brede *.json/*.csv-regel
# hierboven is een privacy-vangnet voor energie-exports, maar ving per ongeluk deze essentiële
# bestanden (package.json/-lock.json stonden daardoor NIET in de repo → `npm install` faalt).
!serve.json
!package.json
!package-lock.json

# Claude project bestanden
.claude/
cline_mcp_settings.json

# Cline/AI assistant bestanden
assistant/

# macOS
.DS_Store

# Editor
.vscode/
.idea/

# Lokale serverbestanden
*.log

# Lokale preview-symlink (/energie/ pad-prefix) — nooit committen (recursief)
/energie
node_modules/
dist/

# Build output (gegenereerd door esbuild uit src/app.js)
/app.js
/app.js.map
